export interface Actuals {
    Slug: string;
    Tytul: string;
    Tresc: string;
    Dodano: string;
}

export interface News {
    slug: string;
    title: string;
    description: string;
    date: string;
}